# Gemini (Google AI Studio) — never commit real keys
GEMINI_API_KEY=your_key_here
# flash-lite = faster; flash = smarter
GEMINI_MODEL=gemini-2.5-flash-lite
GEMINI_FALLBACK_MODEL=gemini-2.5-flash

# Service port (local)
PORT=8002
